# My stack

The Tool Scout routine reads this file every Monday. Anything listed under
"Already use or know" is never recommended. Edit it freely, or reply to the
Monday email with "I also use X" and the routine adds it here.

## Profile

- Two Linux machines (desktop + notebook): Manjaro/BigLinux, KDE Plasma 6 on
  **X11**, zsh with oh-my-zsh (only the `git` plugin). The machines stay in
  sync with Syncthing over Tailscale.
- Work: backend developer on a SaaS product. Python, Postgres, Docker-based
  local environments, AWS, GitHub PRs gated by a 15-job CI pipeline, git
  worktrees.
- Languages: **Python** and **JS/TS** (also an Astro blog).
- Heavy AI-agent user: runs several Claude Code / Codex / Kiro sessions at once
  in herdr panes, and hands work off between the two machines.
- Side interests (known, but outside the main recommendation scope): pentest
  and infosec, SDR, electronics (KiCad, Arduino), Pwnagotchi, retro gaming.

## Preferences

- **In scope:** Dev & terminal · AI tooling · Desktop & productivity (Linux/KDE).
- **Out of scope** (wildcard slot only): self-hosted, security, hardware.
- **Biggest pain points** (weight picks toward these):
  1. Shell & navigation: jumping between dirs/repos, finding files, logs, remembering commands
  2. Git & code review: worktrees, PR review, diffs, conflicts, CI
  3. Running AI agents: juggling many sessions, context, cost, handoffs between machines
- **Form:** no preference between TUI/CLI and GUI.
- **Price:** free or freemium only. Skip tools that are paid-only.
- **Maturity:** a mix. Mostly tools that gained traction in the last few months,
  plus an occasional brand-new project clearly labelled *experimental*.
- **Platform:** must run on Linux, and on X11 when it's a GUI or desktop tool.
  Skip macOS/Windows-only and Wayland-only tools.

### Learned from feedback

<!-- The routine appends taste notes here based on email replies. -->

## Already use or know (never recommend)

- **Terminal & shell:** tmux, herdr, atuin, fzf, bat, ripgrep, jq, zsh, oh-my-zsh, Konsole, AshyTerm, htop, nvtop
- **Editors & IDEs:** VS Code, Cursor, Kiro (IDE + CLI), Orca IDE, Kate
- **AI:** Claude Code, Codex CLI, opencode, LM Studio, ai-memory
- **DB & API clients:** DBeaver, Insomnia, Postman
- **Dev infra:** Docker (+ compose, buildx), GitHub CLI (gh), AWS CLI, Playwright, Semgrep, gitleaks, Node/npm, Go
- **Desktop & productivity:** Obsidian, RustDesk, Syncthing, Tailscale, KDE Connect, Klipper, Timeshift, AppImageLauncher, Brave, Chrome, Firefox, Spotify, mpv, GIMP, Okular, qBittorrent, fcitx5
- **Security:** Burp Suite, nuclei, ffuf, feroxbuster, sqlmap, nmap, testssl.sh, wafw00f, whatweb, Wireshark
- **Hardware & radio:** KiCad, Arduino IDE, SDR++, rtl-sdr, Pwnagotchi
- **Gaming:** Steam, Lutris, RetroArch

## Known gaps

These classics are not installed: zoxide, starship, eza, fd, lazygit,
git-delta, btop, uv, mise, direnv. They're allowed, but they're "proven" rather
than new, so include **at most one classic per email**.
